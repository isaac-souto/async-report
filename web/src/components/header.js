const Header = () => {
  return (
    <div className='lg:text-center'>
      <h2 className='text-lg text-indigo-600 font-semibold'>
        ReactJS + TailwindCSS + .Net Core + SignalR + RabbitMQ + Minio
      </h2>
      <p className='mt-4 max-w-2xl text-xl text-gray-500 lg:mx-auto'>
        Exemplo de geração de relatórios de forma assíncrona com envio da
        notificação com link para o usuário realizar o download após a conclusão
        do processo.
      </p>
    </div>
  )
}

export default Header
